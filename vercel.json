{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "cd carelens-frontend && npm install && npm run build",
  "outputDirectory": "carelens-frontend/dist",
  "installCommand": "cd backend && npm install && cd ../carelens-frontend && npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/backend/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}